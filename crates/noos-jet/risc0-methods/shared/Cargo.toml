[package]
name = "noos-jet-risc0-shared"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
blake3 = { version = "1.8.5", default-features = false, features = ["pure"] }
