# G3 evidence location

No public-duration evidence is checked in. A real run appends canonical one-line JSON
checkpoints to `checkpoints.ndjson` only after the active exact-revision manifest and
the required independent operator signatures exist. Corrections are new signed
records; prior lines are never edited, removed, or waived. Local test fixtures and
simulated clocks are never copied here.
