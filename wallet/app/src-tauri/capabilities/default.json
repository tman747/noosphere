{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core invoke plumbing only; no fs, shell, or network capabilities.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
